<Page x:Class="UiNutriguia.Views.Pages.SchedulerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:UiNutriguia.Views.Pages"
      xmlns:models="clr-namespace:UiNutriguia.Models"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance local:SchedulerPage,
                           IsDesignTimeCreatable=False}"
      d:DesignHeight="450" d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      Title="SchedulerPage">

    <Page.Resources>
        <!-- Estilo personalizado para aumentar el tamaño de los botones de días en el calendario -->
        <Style TargetType="CalendarItem">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Calendar para seleccionar fechas -->
        <Calendar SelectedDate="{Binding ViewModel.SelectedDate}"  Grid.Column="0" />

        <!-- Lista de citas -->
        <StackPanel Grid.Column="1" Margin="10">
            <ui:TextBlock FontTypography="Title" Text="{Binding ViewModel.SelectedDateOnly}" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding Appointments}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:AppointmentModel}">
                        <Grid Margin="8,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <Border Background="{ui:ThemeResource ControlStrongStrokeColorDefaultBrush}">
                                <TextBlock Text="{Binding StartHour}"></TextBlock>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Botón para agregar una nueva cita -->
            <Button Content="Agregar Cita" Command="{Binding AddAppointmentCommand}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
